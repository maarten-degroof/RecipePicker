<resources>

    <!-- Base application theme. -->
    <style name="Basic" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="android:windowActionBarOverlay">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
        <!-- Enable window content transitions -->
        <item name="android:windowActivityTransitions">true</item>
    </style>

    <!-- android:statusBarColor takes care of the top bar where the system icons are placed -->
    <style name="AppTheme" parent="Basic">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/primaryLightColor</item>
        <item name="android:statusBarColor">@color/primaryDarkColor</item>
    </style>
    
    <style name="AppTheme.Launcher">
        <item name="android:windowBackground">@drawable/launch_screen</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">?android:attr/listDivider</item>
    </style>

    <style name="ToolbarStyle">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:colorBackground">@color/primaryColor</item>
        <item name="colorAccent">@color/primaryLightColor</item>
        <item name="colorControlNormal">@android:color/white</item>
        <item name="android:textColorHint">@android:color/white</item>
    </style>

    <style name="OverflowToolbarStyle">
        <item name="android:textColorPrimary">@android:color/black</item>
    </style>

    <!-- This is used for difficulty chip on the viewRecipeActivity -->
    <style parent="Widget.MaterialComponents.Chip.Action" name="BiggerChip">
        <item name="chipCornerRadius">1dp</item>
        <item name="chipMinHeight">40dp</item>
        <item name="android:textStyle">bold</item>
        <item name="chipBackgroundColor">#FFFFFFFF</item>
        <item name="android:textAppearance">@style/LargerText</item>
    </style>
    <style parent="TextAppearance.AppCompat.Large" name="LargerText">
        <item name="android:textSize">20sp</item>
    </style>

</resources>
